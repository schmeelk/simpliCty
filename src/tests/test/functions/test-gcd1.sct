int gcd(int a, int b) {
  while (a != b) {
    if (a > b) a = a - b;
    else b = b - a;
  }
  return a;
}

int main()
{
  putchar(gcd(65,65));
  putchar(gcd(66,66));
  putchar(gcd(67,67));
  putchar(65);
  return 0;
}
